connection figure figure set start connector connector nbsp start connector get start connector set end connector connector nbsp end connector get end connector update connection can connect figure nbsp start figure nbsp end can connect figure nbsp start connect same connection figure nbsp other set start point java awt geom point2 d nbsp p set end point java awt geom point2 d nbsp p set point nbsp index java awt geom point2 d nbsp p get point count java awt geom point2 d get point nbsp index java awt geom point2 d get start point java awt geom point2 d get end point figure get start figure figure get end figure liner get liner lineout set liner liner nbsp value all superinterface java lang cloneable dom storable figure java io serializable all know implement dependency figure label line connection figure line connection figure connection figure figure set the start connector of the connection set to null to disconnect the start connection parameter start the start figure of the connection parameter start the start figure of the connection get the start connector null there is no start connection set the end connector of the connection set to null to disconnect the end connection parameter end the end figure of the connection parameter end the end figure of the connection get the end connector null there is no end connection update the connection f i x m e what da do? check two figure can be connect using connection figure implement method to constrain the allow connection between figure check connection figure can be attach to the provide start figure is use to provide an early feedback to the user when he she create a connection check the connection figure connect the same figure f i x m e what we need for? set the start point set the end point set the specify point get the point count the specify point get the start point specify by get start point in figure see also figure basic set bound java awt geom point2 d java awt geom point2 d specify by get start point in figure see also figure basic set bound java awt geom point2 d java awt geom point2 d get the end point specify by get end point in figure see also figure basic set bound java awt geom point2 d java awt geom point2 d specify by get end point in figure see also figure basic set bound java awt geom point2 d java awt geom point2 d get the start figure of the connection is a convenience method get start connector get owner and handling null get the end figure of the connection is a convenience method get end connector get owner and handling null get a liner object which encapsulate a lineout algorithm figure typically a liner access the child component of figure and arrange their graphical presentation lineout strategy use by figure lineout strategy use by figure a lineout algorithm is use to define how the child component should be lay out in relation to each other the task lineout the child component presentation is delegate to a liner which can be plug in at runtime set a liner object which encapsulate a lineout algorithm figure typically a liner access the child component of figure and arrange their graphical presentation it is a good idea to set the liner in the initialize method so it can be recreate a graphical composite figure is read and restore from a storable input stream parameter value encapsulation of a lineout algorithm parameter value encapsulation of a lineout algorithm 